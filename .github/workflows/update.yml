name: Update public events

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prepare environment
        run: python --version
      - name: Collector placeholder
        run: echo "collector stage will be enabled after source adapters"
